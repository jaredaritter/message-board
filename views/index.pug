extends layout

block content
  h1= title
  a(href="/new") New Message
  if messages
    each message in messages
      .message
        h4 #{message.user} - #{message.added}
        p #{message.text}